{
  "name": "fastify-ddd",
  "scripts": {
    "build": "tsc",
    "start": "docker compose up -d && tsc && cp ./.env.example ./dist && node ./dist/main.js"
  },
  "type": "module",
  "dependencies": {
    "@fastify/auth": "^5.0.2",
    "@fastify/bearer-auth": "^10.1.1",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.1.0",
    "@fastify/env": "^5.0.2",
    "@fastify/helmet": "^13.0.1",
    "@fastify/jwt": "^10.0.0",
    "@fastify/mongodb": "^9.0.2",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
